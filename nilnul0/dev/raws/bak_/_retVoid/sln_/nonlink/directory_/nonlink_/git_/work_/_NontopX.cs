using nilnul.dev;

using System;
using System.Collections.Generic;
using System.Diagnostics;
using System.IO;
using System.Linq;
using System.Text;
using System.Threading;
using System.Threading.Tasks;

namespace nilnul.dev.raws.bak_._retVoid.sln_.nonlink.directory_.nonlink_.git_.work_
{
	static public class _NontopX
	{
		public static void _Exe(
			string folder,
			CancellationToken token,
			nilnul.win.prog_.Git git = null
		)
		{
			git_._Plain0nontopX._Exe(folder,token,git);
			/*
			if (
				nilnul.fs.git.module._ignore_.rule_.nn_._IntendGitX.IsIntend_ofShieldAddress(folder)
			)
			{
				sln_.nonlink.directory_.nonlink_.git_.plain0nontop_._IntentX.Exe(folder, token, git);
			}
			else
			{
				div_.git_.plain0nontop_.undone_._NonIntentX._Exe(folder, token, git);

			}
			*/
			//_retVoid.SettingX.shieldsNewlyBaked.Add(folder);

		}




	}
}
