using nilnul.dev;

using System;
using System.Collections.Generic;
using System.Diagnostics;
using System.IO;
using System.Linq;
using System.Text;
using System.Threading;
using System.Threading.Tasks;

namespace nilnul.dev.raws.bak_._retVoid.sln_.nonlink.directory_.nonlink_.git_
{
	static public class _Plain0nontopX_serial
	{
		public static void _Exe(
			string folder,
			CancellationToken cancel,
			nilnul.win.prog_.Git git = null
		)
		{
			//try
			//{
				if (
				nilnul.fs.git.module._ignore_.rule_.nn_._IntendGitX.IsIntend_ofShieldAddress(folder)
			)
				{
					plain0nontop_._Intent_serialX.Exe(folder, cancel, git);
				}
				else
				{
					plain0nontop_._NonIntentX__serial._Exe(folder, cancel, git);

				}

				//if (
				//	nilnul.dev.raws.bak_._retVoid.SettingX.IsBaked(folder)
				//)
				//{
				//	Trace.TraceInformation($" {folder} already done last time");
				//}
				//else
				//{
				//	sln_.nonlink.div_.nonlink_.plain0nontop_.._UndoneX._Exe(folder, cancel, git);

				//}

			//}
			//catch (Exception e)
			//{
			//	Trace.TraceError($"exception when baking {folder}:{e}");
			//}

		}



	}
}
