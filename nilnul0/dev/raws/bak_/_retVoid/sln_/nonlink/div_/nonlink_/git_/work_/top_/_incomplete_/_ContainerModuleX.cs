using System.Diagnostics;
using System.Threading;

namespace nilnul.dev.raws.bak_._retVoid.sln_.nonlink.div_.nonlink_.git_.work_.top_._incomplete_
{
	/// <summary>
	/// </summary>
	public static class _ContainerX
	{
		/// <summary>
		/// </summary>
		/// <param name="git"></param>
		/// <param name="_location"></param>
		/// <param name="log"></param>
		/// <param name="err"></param>
		/// <remarks>
		/// </remarks>
		/// <returns>
		/// newly processed
		/// </returns>

		public static void _Exe(
			string folder,
			/*srcs._bak.Io log,*/ CancellationToken cancel,
			nilnul.win.prog_.Git git = null
		)
		{
			Trace.TraceInformation($"in {typeof(_ContainerX).FullName} {nameof(_Exe)}  for {folder} ");

			if (nilnul.dev.raws.bak_._retVoid.SettingX.IsModuleBaked(folder))
			{
				Trace.TraceInformation($"module {folder} is already baked previously");
				//incomplete_._ModuleDoneX._Exe(folder, cancel);
			}
			else
			{

				nilnul.dev.raws.bak_._retVoid.sln_.nonlink.div_.nonlink_.git_.work_.top_._incomplete_.container_._UndoneX._Exe(folder, cancel);
			}
			//sln_.nonlink.div_.nonlink_.git_.work_.top_.incomplete_._moduleUndone_.module_._container_._PrimeX._Exe(folder, cancel, git);

			//container_._PrimedX._Exe(folder, cancel, git);



		}
	}
}
