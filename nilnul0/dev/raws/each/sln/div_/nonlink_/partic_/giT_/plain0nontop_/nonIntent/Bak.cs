using System;
using System.Collections.Concurrent;
using System.Diagnostics;
using System.Threading;

namespace nilnul.dev.sln_.nonlink.div_.nonlink_.partic_.git_.plain0Nontop_.nonIntent
{
	/// <summary>
	/// nonIntent so it's not a sln.
	/// </summary>
	public  class Bak
	{
		private nilnul.fs.address_.shields.dev_._bak.Io _cfg;
		public nilnul.fs.address_.shields.dev_._bak.Io cfg
		{
			get { return _cfg; }
			set { _cfg = value; }
		}
		public ConcurrentBag<nilnul.fs._address.DivI> innerModules;


		public Bak(nilnul.fs.address_.shields.dev_._bak.Io cfg):this(cfg,new ConcurrentBag<fs._address.DivI>())
		{

		}

		public Bak(
			nilnul.fs.address_.shields.dev_._bak.Io cfg
			,         
			ConcurrentBag<nilnul.fs._address.DivI> innerModules
)
		{
			_cfg = cfg;
			this.innerModules = innerModules;
		}


		internal void _Exe(string _location__nonrootWork
			)
		{
			new partic_.nonintent_.git_.plain0Nontop.Bak(cfg, innerModules)._void(_location__nonrootWork);


		}


	}
}
