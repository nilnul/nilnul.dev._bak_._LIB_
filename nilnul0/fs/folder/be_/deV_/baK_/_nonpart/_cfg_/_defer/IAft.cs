using System;

namespace nilnul.fs.folder.be_.deV_.baK_._neglect._cfg_._halt
{
	/// <summary>
	/// return true if current is aft given date; that is: unneglected.
	/// return false if current is before given date; that is neglected
	/// </summary>
	///
	[Obsolete(nameof(IHalt) + " as a semantic anto is intuitive in neglect context.")]
	public interface IAft { }
}
